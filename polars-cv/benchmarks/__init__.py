"""
Benchmarking suite for polars-cv.

This package provides comprehensive benchmarks comparing polars-cv
against other vision processing frameworks like OpenCV, PIL, and torchvision.
"""

from __future__ import annotations

# No `__version__` here: the benchmark suite is not released separately, and a
# second version literal only drifts. Use `polars_cv.__version__`.
