# Root Cargo.toml
[workspace]
members = ["view-buffer", "polars-vision"]
resolver = "2"

# Workspace-level profile settings
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true